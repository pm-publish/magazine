{{ set(this, 'title', 'Signup Successful') }}
{% set networkData = _Network.getNetworkData() %}
{% set config       = _Network.getThemeConfig() %}
{% set thankyou = {} %}
{% if config['thank-you-page'] %}
        {% set thankyou = config['thank-you-page'] %}
{% endif %}

<main class="site-main">
        <div class="c-thanks">
                <div class="container">
                        <div class="row">
                                <div class="col-12">
                                        <div class="c-thanks__main">
                                                {% if thankyou['headingText'] is defined and thankyou['headingText'] != '' %}
                                                        <h2 class="c-thanks__main-heading">{{thankyou['headingText']}}</h2>
                                                {% else %}
                                                        <h2 class="c-thanks__main-heading">Thanks for submitting an event!</h2>
                                                {% endif %}

                                                {% if thankyou['descriptionText'] is defined and thankyou['descriptionText'] != '' %}
                                                        <p class="c-thanks__main-desTest">{{thankyou['descriptionText']}}</p>
                                                {% endif %}

                                                <div class="buttons-container">
                                                        <a href="/event-calendar" class="c-button c-button--lightBlue">Event listings</a>
                                                        <a href="/" class="c-button c-button--lightBlue-bordered">Home</a>
                                                </div>
                                        </div>
                                </div>
                        </div>
                </div>
        </div>
</main>